# Install torch FIRST with specific version
torch==2.0.0
torchaudio==2.0.0

# Then install WhisperX from PyPI
whisperx

# Pin pyannote.audio to avoid conflicts
pyannote.audio==3.1.1

# Other dependencies
numpy<2.0  # Prevent numpy 2.x
pydub
ffmpeg-python
openai
python-dotenv
pandas

# Commented for HF Spaces
# fastapi
# uvicorn[standard]
